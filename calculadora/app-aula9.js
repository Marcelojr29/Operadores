/*
document.getElementById('calcular').addEventListener("click", function(){

    let num1 = Number(document.getElementById('num1').value); // Pegando de atributo (value), da tag option
    let num2 = Number(document.getElementById('num2').value); // Pegando de atributo (value), da tag option

    let operacao = document.getElementById('operacao').value; // Essa variável irá retornar: soma, subtração, multiplicação ou divisão
    let resultado = 0;


    if(resultado == 'soma'){
        resultado = num1 + num2;
    } else if(resultado == 'subtracao'){
        resultado = num1 - num2;
    } else if(resultado == 'multiplicacao'){
        resultado = num1 * num2;
    } else(resultado == 'divisao');{
        resultado = num1 / num2;
    }

    document.getElementById('resultado').innerHTML = 'Resultado do calculo: ' + resultado;
});
*/
/**  Faça a estrutura do calculo usando if-else if-else e retorne a soma
 * , subtracao, multiplicacao ou divisao dos dois numeros */



 //